id: hrc-runtime.asp-toolchain-selection
kind: invariant
status: active
owner_project: hrc-runtime
scope: HRC-owned ASP child-process and aspd-prepared worker selection and compatibility boundary
predicate: >
  Except for the configured aspd-prepared route below, every HRC-owned ASP child
  spawn resolves its binary at spawn time through one selection authority. A
  non-empty per-binary command override wins, then the binary's canonical name
  under an explicitly configured absolute HRC_ASP_TOOLCHAIN_ROOT, then the HRC
  release's resolveHoistedBinary result. No such spawn path, including the
  legacy interactive stdio broker path, independently selects or caches a
  command; injected constant commands are test seams only. For HRC-hosted
  headless codex-app-server with operator presentation none and HRC_ASPD_SOCKET
  configured, HRC spawns no ASP compiler child and launches the worker only from
  the executionRelease executable and argv prefix committed under
  hrc-runtime.aspd-prepared-execution-release. That route never consults a
  per-binary override, HRC_ASP_TOOLCHAIN_ROOT, resolveHoistedBinary, PATH, a
  moving current link, or source, and it has no fallback to them. A
  configured-root selection that is missing or not executable fails before
  spawn, and incompatible facade, aspd, or broker protocol/capability hellos
  fail closed before invocation work. Admin status derives effective source and
  path for resolver-governed routes from that resolver, keeps the aspd service
  projection distinct, and distinguishes both from the immutable bundled ASP
  tuple governed by hrc-runtime.observable-release. The root is one
  configuration namespace, not an atomic build-identity boundary, and the
  daemon's in-process ASP imports remain pinned to its release.
authority: wrkq:T-08542 architecture approval, 2026-09-16; wrkq:T-07764 rev 2 architecture approval, 2026-08-30
source:
  - wrkq:T-08542
  - commit:e5d2c31f
  - docs/aspd-headless-codex-integration.md
  - architecture/records/invariants/hrc-runtime.aspd-prepared-execution-release.yaml
  - wrkq:T-07764
  - architecture/records/invariants/hrc-runtime.observable-release.yaml
  - packages/hrc-server/src/hoisted-binary.ts
  - packages/hrc-server/src/option-resolvers.ts
  - packages/hrc-server/src/broker-interactive-handlers/substrate-allocator.ts
  - packages/hrc-server/src/broker/controller.ts
  - packages/hrc-server/src/broker/controller/dispatch.ts
  - packages/hrc-server/src/broker/capabilities.ts
required_tests:
  - just architecture-records
  - T-08542 configured aspd route bypasses every override root bundled PATH current and source resolver while all other routes retain the existing precedence and status projection
  - T-08542 aspd outage incompatibility and missing execution release refuse without resolver fallback or worker hosting
  - T-07764 per-spawn precedence and configured-root refusal gates across facade, durable broker, and legacy stdio paths
  - T-07764 protocol/capability remedy and status/resolver authority gates
  - T-07764 live durable and legacy external-root round trips plus bundled rollback proof
last_verified: 2026-09-16 T-08542 design approval; implementation and installed gates pending
reopen_when: An ASP child spawn site, aspd-prepared route, selection precedence, external-root or execution-release semantics, compatibility handshake, toolchain or aspd status projection, or in-process ASP dependency boundary changes.
supersedes: []
superseded_by: []

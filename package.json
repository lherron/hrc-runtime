{
  "name": "agent-spaces-v2",
  "private": true,
  "workspaces": ["packages/*", "integration-tests"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:ci": "bun run --filter 'spaces-core' build && bun run --filter 'spaces-git' --filter 'spaces-claude' build && bun run --filter 'spaces-resolver' build && bun run --filter 'spaces-store' build && bun run --filter 'spaces-materializer' build && bun run --filter 'spaces-lint' build && bun run --filter 'spaces-engine' build && bun run --filter '@lherron/agent-spaces' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun run --filter '*' test",
    "test:integration": "bun test integration-tests/tests",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "lefthook": "^1.11.5",
    "typescript": "^5.7.2"
  }
}

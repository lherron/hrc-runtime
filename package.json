{
  "name": "agent-spaces-v2",
  "private": true,
  "workspaces": ["packages/*", "integration-tests"],
  "scripts": {
    "build": "bun run --filter '*' build",
    "build:ci": "bun run --filter '@agent-spaces/core' build && bun run --filter '@agent-spaces/git' --filter '@agent-spaces/claude' build && bun run --filter '@agent-spaces/resolver' build && bun run --filter '@agent-spaces/store' build && bun run --filter '@agent-spaces/materializer' build && bun run --filter '@agent-spaces/lint' build && bun run --filter '@agent-spaces/engine' build && bun run --filter '@lherron/agent-spaces' build",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun run --filter '*' test",
    "test:integration": "bun test integration-tests/tests",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "lefthook": "^1.11.5",
    "typescript": "^5.7.2"
  }
}

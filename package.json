{
  "name": "hrc-runtime",
  "version": "0.5.13",
  "private": true,
  "workspaces": ["packages/*"],
  "scripts": {
    "sync:asp": "bun scripts/sync-asp-from-verdaccio.ts",
    "sync:wrkq": "bun scripts/sync-wrkq-from-verdaccio.ts",
    "build": "bun run build:ordered",
    "build:ordered": "bun run --filter 'agent-action-render' build && bun run --filter 'hrc-core' build && bun run --filter 'hrc-events' build && bun run --filter 'hrc-store-sqlite' build && bun run --filter 'hrc-capture-verifier' build && bun run --filter 'hrc-sdk' build && bun run --filter 'hrc-frame-render' build && bun run --filter 'hrc-server' build && bun run --filter 'hrc-top' build && bun run --filter 'hrc-cli' build && bun run --filter 'hrcchat-cli' build",
    "build:parallel": "bun run --filter '*' build",
    "check": "bun run check:boundaries && bun run check:manifests && bun run lint && bun run typecheck && bun run test",
    "check:boundaries": "bun scripts/check-boundaries.ts",
    "check:manifests": "bun scripts/check-manifest-edges.ts",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bash -lc 'for pkg in \"agent-action-render\" \"hrc-core\" \"hrc-events\" \"hrc-store-sqlite\" \"hrc-capture-verifier\" \"hrc-sdk\" \"hrc-frame-render\" \"hrc-server\" \"hrc-top\" \"hrc-cli\" \"hrcchat-cli\"; do bun run --filter \"$pkg\" test || exit $?; done'",
    "test:fast": "bun run test",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "rebuild": "bun run clean && bun run build",
    "prepare": "lefthook install"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "lefthook": "^1.11.5",
    "smol-toml": "1.6.1",
    "typescript": "^5.7.2"
  }
}

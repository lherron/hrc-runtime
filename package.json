{
  "name": "hrc-runtime",
  "version": "0.5.13",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "sync:asp": "bun scripts/sync-asp-from-verdaccio.ts",
    "sync:wrkq": "bun scripts/sync-wrkq-from-verdaccio.ts",
    "build": "bun run build:ordered",
    "build:ordered": "bun run --filter 'agent-action-render' build && bun run --filter 'hrc-core' build && bun run --filter 'hrc-events' build && bun run --filter 'hrc-store-sqlite' build && bun run --filter 'hrc-capture-verifier' build && bun run --filter 'hrc-sdk' build && bun run --filter 'hrc-viewer' build && bun run --filter 'hrc-frame-render' build && bun run --filter 'hrc-server' build && bun run --filter 'hrc-cli' build && bun run --filter 'hrcchat-cli' build",
    "build:parallel": "bun run --filter '*' build",
    "check": "bun run check:boundaries && bun run check:manifests && bun run check:public-surface && bun run check:suppressions && bun run check:env-hygiene && bun run check:test-size && bun run lint && bun run typecheck && bun run test",
    "check:env-hygiene": "bun scripts/check-env-hygiene.ts",
    "check:boundaries": "bun scripts/check-boundaries.ts",
    "check:architecture-records": "bun scripts/check-architecture-records.ts",
    "check:manifests": "bun scripts/check-manifest-edges.ts",
    "check:public-surface": "bun scripts/check-public-surface.ts",
    "check:suppressions": "bun scripts/check-suppressions.ts",
    "test:size": "bun scripts/check-test-source-size.ts",
    "check:test-size": "bun scripts/check-test-source-size.ts --enforce",
    "typecheck": "bun run --filter '*' typecheck",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "test": "bun run test:fast && bun run test:integration",
    "test:unit": "bash -lc 'export TMPDIR=/tmp; for pkg in \"agent-action-render\" \"hrc-core\" \"hrc-events\" \"hrc-capture-verifier\" \"hrc-frame-render\"; do bun run --filter \"$pkg\" test || exit $?; done'",
    "test:contract": "bash -lc 'export TMPDIR=/tmp; for pkg in \"hrc-store-sqlite\" \"hrc-sdk\" \"hrc-viewer\" \"hrc-cli\" \"hrcchat-cli\"; do bun run --filter \"$pkg\" test || exit $?; done'",
    "test:integration": "TMPDIR=/tmp bun run --filter hrc-server test",
    "test:scripts": "TMPDIR=/tmp bun test scripts/",
    "test:installed-live": "just e2e",
    "test:observability": "bun scripts/test-observability.ts",
    "test:fast": "bun run check:test-size && bun run test:scripts && bun run test:unit && bun run test:contract",
    "test:release": "bun run test:integration",
    "clean": "rm -rf packages/*/dist packages/*/tsconfig.tsbuildinfo",
    "rebuild": "bun run clean && bun run build",
    "prepare": "git rev-parse --git-dir >/dev/null 2>&1 && lefthook install || echo '[prepare] skipping lefthook (not a git checkout)'"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "@types/bun": "^1.1.14",
    "lefthook": "2.1.10",
    "smol-toml": "1.6.1",
    "typescript": "^5.7.2"
  },
  "dependencies": {
    "agent-scope": "latest",
    "agent-harness": "latest",
    "agent-spaces": "latest",
    "cli-kit": "latest",
    "spaces-config": "latest",
    "spaces-execution": "latest",
    "spaces-harness-broker": "latest",
    "spaces-harness-broker-pi-sdk": "latest",
    "spaces-harness-broker-client": "latest",
    "spaces-harness-broker-protocol": "latest",
    "spaces-runtime": "latest",
    "spaces-turn-runner": "latest"
  }
}
